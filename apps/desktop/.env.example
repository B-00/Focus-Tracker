# Frontend env vars (Vite). Copy to .env when overriding defaults.

# Default API origin the desktop app talks to. Can be overridden in the
# desktop app's settings window before pairing (see DesktopApp.md §19).
VITE_API_BASE_URL=http://localhost:3000
