{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main settings window. Covers the Tauri core APIs the frontend uses (invoke for our own commands + opener plugin for launching the web app dashboard).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url"
  ]
}
